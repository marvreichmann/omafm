[package]
name = "omafm"
version = "1.2.0"
edition = "2024"
license = "MIT"
description = "A minimal SDR++ FM receiver for Omarchy"

[dependencies]
serde_json = "1.0"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

